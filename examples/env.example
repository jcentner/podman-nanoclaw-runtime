# =============================================================================
# Nanoclaw Environment Configuration
# =============================================================================
#
# Copy this file into your nanoclaw directory and rename it to .env:
#
#   cp env.example ~/nanoclaw/.env
#
# Then fill in the required values below. At minimum, you need one credential
# (ANTHROPIC_API_KEY or CLAUDE_CODE_OAUTH_TOKEN).
#
# Lines starting with # are comments. Uncomment a line to activate it.
# =============================================================================


# -----------------------------------------------------------------------------
# Credentials (REQUIRED — set at least one)
# -----------------------------------------------------------------------------

# Anthropic API key for Claude. Required if not using OAuth.
# Get one at: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=

# Claude Code OAuth token. Alternative to ANTHROPIC_API_KEY.
# Obtained via `claude login` in the Claude Code CLI.
# CLAUDE_CODE_OAUTH_TOKEN=


# -----------------------------------------------------------------------------
# Assistant Configuration
# -----------------------------------------------------------------------------

# Trigger word for the assistant in group chats.
# Users mention this name to invoke the agent (e.g., "@Andy help me with...").
ASSISTANT_NAME=Andy

# Whether the assistant has its own dedicated phone number.
# Set to true if the WhatsApp account is exclusively for the assistant.
# When true, the assistant responds to all messages (no trigger word needed).
ASSISTANT_HAS_OWN_NUMBER=false


# -----------------------------------------------------------------------------
# Container Configuration
# -----------------------------------------------------------------------------

# Agent container image name and tag.
# Built by build-agent-image.sh using nanoclaw's container/Dockerfile.
# CONTAINER_IMAGE=nanoclaw-agent:latest

# Maximum time (in milliseconds) an agent container can run before being killed.
# Default: 1800000 (30 minutes).
# CONTAINER_TIMEOUT=1800000

# Time (in milliseconds) of inactivity before an agent container is torn down.
# Default: 1800000 (30 minutes).
# IDLE_TIMEOUT=1800000

# Maximum size (in bytes) of agent output captured from stdout.
# Default: 10485760 (10 MB).
# CONTAINER_MAX_OUTPUT_SIZE=10485760


# -----------------------------------------------------------------------------
# Platform / Messaging
# -----------------------------------------------------------------------------

# Timezone passed to agent containers via the TZ environment variable.
# Uses IANA timezone names. Detected automatically from the host if unset.
# TZ=UTC


# -----------------------------------------------------------------------------
# Advanced (usually no need to change)
# -----------------------------------------------------------------------------

# Path to the nanoclaw project root, used for read-only mounts into containers.
# Defaults to the current working directory.
# PROJECT_ROOT=.

# Enable debug/verbose logging from the host process.
# DEBUG=false
